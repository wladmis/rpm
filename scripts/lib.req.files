#!/bin/sh -efu
while IFS=$'\t' read -r f t; do
	case "$t" in
		*ELF*' shared object'*)
			echo "$f" ;;
		*ELF*' executable'*'dynamically linked'*)
			[ -x "$f" ] && echo "$f" ||
			echo "${0##*/}: ELF executable $f is not executable" >&2 ;;
	esac
done
